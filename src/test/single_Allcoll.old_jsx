import React from 'react';

export default class single_Allcoll extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        if (this.props._Debuge_Show_Code) {
            return (
                <>{this.props.el.key_value + " [ " + this.props.el.main_type + " / " + this.props.el.key + " / " + this.props.el.isDVC + " ]"}</>
            );
        } else {
            return (<>{this.props.el.key_value}</>);
        }
    }
}